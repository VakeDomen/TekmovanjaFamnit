import * as express from 'express';
const path = require("path");

const router: express.Router = express.Router();
module.exports = router;


router.get('/upload/:id', (req: any, resp: express.Response) => {

});


  